%h2= t ".title", :id => @node.osm_id

.row
  .span6
    %h4= t ".tags"
    %dl.dl-horizontal
      - @node.ordered_tags.each do |k,v|
        %dt= k
        %dd= v
  .span6
    %h4= t ".status"
    = status_label(@node)

= render "shared/entity_map", :entity => @node
