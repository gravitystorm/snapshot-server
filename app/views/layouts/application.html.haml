!!!
%html
  %head
    %title= t ".title"
    = stylesheet_link_tag "application"
    = javascript_include_tag "application"
    = csrf_meta_tag
  %body
    .container
      %h1= link_to t(".title"), root_path
      %p= link_to t(".sign_out"), destroy_admin_session_path, method: "delete"
      .flash-container
        - [:alert, :notice].each do |type|
          - if flash.key? type
            .flash{class: type}
              = flash[type]
      = yield
